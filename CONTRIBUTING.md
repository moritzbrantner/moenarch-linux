# Contributing

Keep changes narrow and preserve the distribution ownership boundary in `ARCHITECTURE.md`.

For image changes, validation should cover both static profile checks and a real boot of the generated artifact. Do not treat an ISO build alone as evidence that the image is usable.

Generated ISO files, work directories, and build outputs must not be committed.
